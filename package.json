{
  "name": "bz",
  "version": "0.0.0",
  "devDependencies": {
    "esbuild": "^0.17.15",
    "jest": "^29.5.0",
    "jest-environment-miniflare": "^2.13.0",
    "miniflare": "^2.13.0",
    "wrangler": "2.12.3"
  },
  "type": "module",
	"module": "./dist/index.mjs",
  "private": true,
  "scripts": {
		"build": "esbuild src/index.js --bundle --sourcemap --target=esnext --format=esm --outfile=dist/index.mjs",
    "start": "wrangler dev",
    "deploy": "wrangler publish",
    "test": "npm run build && node --experimental-vm-modules node_modules/jest/bin/jest.js"
  }
}
